
using std::string;

class Money {
public:

  Money();
  // Money(int joints);
  // Money(int joints, int talons);
  // Money(int joints, int talons, int marks);
  ~Money();

private:

  int drabs;
  int joints;
  int talons;
  int marks;

};
